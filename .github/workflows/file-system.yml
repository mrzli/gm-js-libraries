name: file-system

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Latest Commit
        uses: actions/checkout@v2
      - name: Cache Node Modules
        uses: actions/cache@v2
        with:
          path: 'packages/file-system/node_modules'
          key: ${{ runner.os }}-node-${{ hashFiles('packages/file-system/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: 16.x
          registry-url: https://npm.pkg.github.com/
      - name: Install Node Modules
        run: |
          cd packages/file-system
          npm ci
      - name: Lint
        run: |
          cd packages/file-system
          npm run lint
          npm run prettier:check
      - name: Build Project
        run: |
          cd packages/file-system
          npm run build
      - name: Prepare for Publish
        run: |
          cd packages/file-system
          echo "registry=https://npm.pkg.github.com/@mrzli" > .npmrc
      - name: Publish
        run: |
          cd packages/file-system/dist
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
